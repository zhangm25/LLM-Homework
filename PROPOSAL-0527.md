# RoamMind —— 对话式行程规划助手 · 项目 Proposal

> 项目名：**RoamMind**（"随心行" · 对话式行程规划助手）
> 课程：《生成式大模型应用》大作业 · 交付物为**有展示度的 Demo**
> 文档版本：v0.9（2026-05-26）｜ 用途：作为后续 vibe coding 的需求与设计依据（已融合队友需求书）

---

## 0. TL;DR（一句话愿景）

做一个像特斯拉车机 Grok 那样的**对话式行程规划助手**：用户用一句自然语言说出需求——**无论是清晰具体的（"先去 A，再吃 B，最后去 C，回家"），还是模糊带情绪的（"今天有点累，想找个安静的地方待着"）**——助手都能理解、（必要时）追问、给出一条**由真实地点组成、时间上跑得通**的行程（出行方式按距离动态选择：步行/驾车/公交等），在精致的网页上用**地图 + 时间轴**呈现，并支持**一键唤起高德 App 直接导航**。演示以北京为主，但设计上**不绑定城市、可泛化**。

需求不止"休闲玩乐"——也涵盖**带任务的一天**（吃饭/购物/接人/**开会**…）与**带固定日程**（如某时某地的会议、给定议程表）的复杂行程；演示主打情绪化休闲场景，**"出差/开会的复杂一天"作为第二个卖点场景**，模型按"任务"建模、两者通吃。

**北极星指标：惊艳性 + 展示度。** 本文档所有取舍都服从这一条。

---

## 1. 动机与背景

灵感来自特斯拉车机 AI 助手 Grok 的演示：车主用自然语言表达出行意愿，助手分析需求、给出方案并规划好路线与时间。我们想用大模型复刻并本地化这一体验。

为什么这是个好的大作业题目：
- **一眼就懂，做好了很惊艳**——天然适合现场演示。
- **难度有梯度**——从"能跑通"到"很惊艳"之间有大量可打磨空间，时间充裕时上限很高。
- **技术栈现代且现成**——大模型 function calling + 地图开放 API + 现代前端，正好覆盖"生成式大模型应用"的核心能力。

**演示锚定北京**（高德国内数据最好、演示可信度高），但**设计上不绑定特定城市**，可泛化到其它城市；**出行方式按距离动态推荐**（近则步行、远则驾车/公交），不写死"驾车"。

---

## 2. 设计原则（贯穿全文的取舍准则）

1. **惊艳优先**：每一个特性都用"它能不能让演示炸场"来打分排序。
2. **真实不编造（反幻觉是生死线）**：所有推荐地点必须来自高德**真实检索**，模型只负责"挑选与排序"，绝不允许"凭空报店名"。
3. **可解释 + 显智能**：把模型对"情绪/模糊需求"的理解**显式展示**出来；并通过**主动澄清**与**交互式重规划**，让"它真的听懂了、会思考、很通人性"这件事被用户直接看见。
4. **体验顺滑但不浮夸**：漂亮的可视化要服务于"行程真的合理"——一个好看但时间兜不拢的方案会瞬间破功，所以保留最低限度的真实耗时校验。
5. **接口预留、分层解耦**：文本链路先做扎实；语音作为高价值进阶项，架构上预留接入点，不返工。

---

## 3. 问题定义

**输入**：自然语言（文本，进阶支持语音）；支持多轮对话。意图可为：
- **清晰型**：显式给出地点、顺序、时间、起终点。
- **模糊/情绪型**：只给情绪或抽象偏好（"想静静""随便吃点好的"），需要推断与（必要时）澄清。
- **带任务/带日程型**：含要办的事（吃饭/购物/接人/开会…）或固定约定（某时某地的会议、议程表）、可指定日期；行程需围绕硬时间锚点排（多日行程为扩展）。

**输出**：① 一段**有共情感、可解释**的对话回复；② 网页上的**地图路线可视化 + 时间轴**（分段交通方式/耗时/费用、完整时间表与可行性结论）；③ **一键唤起高德 App 导航**的外链。

**核心子问题（即处理流水线）**：

```
用户输入(文/语音)
   │
   ▼
①意图理解 & 澄清    把需求翻译成结构化"需求/偏好对象"(JSON)；缺关键约束则主动追问，不硬猜
   │
   ▼
②落地检索(grounding) 调高德 POI 搜真实候选 → 按 评分/距离/营业/价位 筛选排序（反幻觉关键步）
   │
   ▼
③轻量时空排程        调高德 距离/路径 → 定顺序 + 排时刻（卡营业时间/用餐时段/停留时长/路上耗时）
   │
   ▼
④表达 & 可视化交付    LLM 生成讲解 + 前端画路线/时间轴 + 高德 URI 唤起导航
        │
        └── 用户反馈("太远/换清静点/预算低些") → 回到①或②增量重规划
```

技术重心：**①意图理解（含澄清）**、**④可视化交付**、以及**交互式重规划**是惊艳与"显智能"的主战场；②是反幻觉地基；③保留轻量真实校验，不堆复杂优化。

---

## 4. 功能需求（FR）—— 按演示惊艳度排序

> 标注：✨=演示炸场点；🅼=核心(必做)；🅿=进阶；🅷=炫技(锦上添花)；🅵=预留(设计预留、本期不开发)

| 编号 | 功能 | 级别 | 说明 |
|---|---|---|---|
| FR-1 | **情绪/模糊需求理解** ✨ | 🅼 | 把"心情不好想放松"翻译成可检索的偏好结构（氛围/品类/时段/规避项），并**在 UI 上把理解结果显式展示**为一张"我读懂了你"的卡片 |
| FR-2 | **多轮主动澄清** ✨ | 🅼 | 关键约束缺失/有歧义时主动追问（合并成自然一句）——**复杂/模糊场景下"会问对问题"最显理解力与通人性**，是核心展示点 |
| FR-3 | **真实 POI 落地检索（反幻觉）** ✨ | 🅼 | 所有地点来自高德实时检索；每个点可点开看真实信息（评分/营业时间/人均/距离/照片）|
| FR-4 | **时空合理的行程排程** | 🅼 | 先广搜候选 → 组合成若干可行路线 → 择优；顺序合理、用餐卡饭点、营业不冲突、耗时取自高德真实数据；默认主推 1 条 + 1~2 备选（见 §7.3）|
| FR-5 | **地图路线可视化 + 时间轴** ✨ | 🅼 | 网页地图上画出整条路线与标点；配带时刻/停留时长的时间轴；路线**动画绘制**入场（克制、优雅）|
| FR-6 | **一键唤起高德 App 导航** ✨ | 🅼 | 生成深链/外链，移动端点击直接调起高德 App 进入（多途经点）路线导航 |
| FR-7 | **流式对话与"思考过程"展示** ✨ | 🅼 | 回复 token 流式输出；检索/规划时展示"正在帮你找安静的去处…"等进度，强化"它在认真为我做事"的观感 |
| FR-8 | **交互式重规划** ✨ | 🅼 | 用户一句"太远了/换个清静的/预算压低点"，系统**增量重排**、地图实时更新——**最能体现复杂场景下的规划能力与通人性**，核心展示点 |
| FR-9 | **语音实时交互** ✨ | 🅷 | 像 Grok 一样"开口说、它开口答"；接 Qwen-Omni-Realtime 实时语音通道 |
| FR-10 | **氛围化 UI（情绪主题）** | 🅷 | 根据心情/时段微调界面色调（如"傍晚看城市"切暖色黄昏主题），增强沉浸感 |
| FR-11 | **任务建模 + 停留时间估计** | 🅼 | 识别任务（吃饭/购物/打球/接人/开会…），按类型库估停留时长与提前量、可改；含"接人/回家"等无需检索的任务 |
| FR-12 | **固定日程锚点 + 指定日期** ✨ | 🅿 | 围绕"某时某地"的会议/约定（含议程表）排程；支持指定日期（多日为扩展）。**"出差/开会的复杂一天"是第二个卖点场景**（见 §9 脚本 D）|
| FR-13 | **分段多模式出行 + 费用 + 换乘** | 🅼 | 每段单独选 步行/驾车/公交（可混合），给耗时、费用与公交换乘信息 |
| FR-14 | **拥堵感知的时间依赖 ETA** | 🅵 | 设计预留、本期不开发：按上一任务结束时刻 + 时段拥堵估算（真未来路况预测视高德支持而定）|
| FR-15 | **可行性 & 冲突检测** | 🅼 | 输出完整时间表，判断任务能否全做完 / 时间是否冲突 / 是否赶得上固定日程 |
| FR-16 | **增值信息 / 贴心提示** | 🅿 | 天气感知、提醒、顺带推荐、相关链接（情绪需求层，见 §7.7）|
| FR-17 | **本地记忆 / 个性化** | 🅵 | 设计预留、本期不开发：记住口味/常去地等做个性化（折中可用 localStorage、无需后端）|

> 注：① **🅵 预留功能**（FR-14 时间依赖 ETA、FR-17 本地记忆/个性化）已做设计预留，**本期不开发**，后续可加。② 原"vs 裸模型对照演示"**已移除**——本项目定位是**产品展示**，把自家功能演示好即可，不做对照实验。

---

## 5. 非功能需求（NFR）

- **NFR-1 响应体验**：首 token < 2s；典型一次完整规划（含工具调用）目标 < 15s；全程有进度反馈，不空等。
- **NFR-2 可靠性**：高德检索为空/超时要有兜底（放宽条件重试、降级提示），绝不"假装有结果"。
- **NFR-3 视觉与设计语言**：走**简洁、克制、高级感**的现代 AI 产品风——参考 **OpenAI / Anthropic** 这类（用户提到的 "Suni" 一并归入此类简洁风）：大量留白、中性色板 + 单一克制强调色、清晰的排版层级、以**对话与地图内容为中心**、动效**微妙不喧哗**（路线绘制等追求"优雅"而非"炫技"）。需达到产品级观感，且**移动端可用**（唤起导航在手机上）。
- **NFR-4 成本可控**：LLM 选便宜档（见 §8），单次规划成本可忽略；高德调用做缓存避免重复请求。
- **NFR-5 可演示性**：内置 2~3 条**预置北京演示脚本**一键触发，保证现场稳定不翻车（同时支持自由输入）。
- **NFR-6 可扩展**：语音等模块以插件式接入，不改主链路。

---

## 6. 系统设计

### 6.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  前端 Web (Next.js + React)                                │
│  · 聊天 UI（流式）   · 高德 JS 地图 + 路线动画              │
│  · "我读懂了你"偏好卡 · 时间轴 · 方案卡片 · 一键导航按钮       │
│  · (进阶) 语音录制/播放                                      │
└───────────────▲───────────────────────┬───────────────────┘
                │ SSE/WebSocket(流式)     │ REST
                │                         ▼
┌───────────────┴─────────────────────────────────────────┐
│  后端 (FastAPI, Python)  —— Agent 编排核心                  │
│  · 意图理解(LLM)  · 澄清策略  · grounding  · 轻量排程        │
│  · LLM function-calling 循环                               │
└──────┬───────────────────────────┬───────────────────────┘
       │                           │
       ▼                           ▼
┌──────────────┐          ┌────────────────────────────────┐
│ DeepSeek-V4  │          │ 高德开放平台                      │
│ (function    │          │ · Web服务API: 搜索POI2.0/路径2.0  │
│  calling)    │          │ · JS API: 前端地图可视化          │
└──────────────┘          │ · URI API/App深链: 唤起导航       │
                          └────────────────────────────────┘
       (进阶) 语音：Qwen-Omni-Realtime  ←WebSocket/WebRTC→ 前端
```

**栈选型一览**：
- 前端：Next.js + React + TypeScript + Tailwind/shadcn-ui；地图用高德 **JS API 2.0**（路线绘制等动效保持克制、优雅；可选用 Loca）。
- 后端：**FastAPI (Python)**（大模型 SDK 生态友好，便于 Agent 编排）；`httpx` 调高德 Web 服务 API。
- LLM：**DeepSeek-V4-Flash**（OpenAI 兼容 SDK，开 function calling）。
- 语音（进阶）：**Qwen-Omni-Realtime**（WebSocket/WebRTC）。

### 6.2 关键数据结构：需求/偏好对象（Intent Object）

①意图理解模块的输出，也是后续所有步骤的"共享上下文"。让 LLM 以 JSON 模式产出：

```jsonc
{
  "explicit_pois": [                 // 用户明确点名的地点（清晰型）
    { "name": "三里屯", "category": "商圈", "fixed_order_index": 0 }
  ],
  "implicit_preferences": {          // 从情绪/模糊表达推断（情绪型，惊艳点）
    "mood": "疲惫 · 想静",
    "vibe_tags": ["安静", "慢节奏", "治愈", "自然"],
    "avoid_tags": ["嘈杂", "排队", "赶时间"],
    "desired_categories": ["brunch/西餐", "公园/书店/咖啡", "城市观景/高处"]
  },
  "date": "today",                         // 指定日期(默认今天)；多日为扩展
  "constraints": {
    "city": "北京",                        // 不绑定城市；由定位/话语推断，可泛化
    "start": { "type": "current|named", "value": "国贸",
               "source": "geolocation|nl_extract" },  // 默认当前位置(定位 或 从话语提取)，稍作确认
    "end":   { "type": "named", "value": "望京" },
    "time_window": { "start": "now", "end": "21:00" },
    "transport": "auto",                   // auto=按距离/场景动态选(walking/driving/transit)，可混合
    "budget_total": null,                  // 总预算；分摊到各 task 消费 + 各段交通成本
    "area_scope": "城内"
  },
  // 把一天拆成"任务"(吃饭/购物/打球/接人/开会/游览…)；任务类型驱动 停留时长 + 是否需检索 POI
  "tasks": [
    { "id":"t1", "type":"dining",      "intent":"brunch",   "dwell_min":90,  "needs_poi":true,
      "time_hint":">=11:00", "explicit":false },
    { "id":"t2", "type":"leisure",     "intent":"安静待着", "dwell_min":120, "needs_poi":true, "explicit":false },
    { "id":"t3", "type":"sightseeing", "intent":"看城市黄昏","dwell_min":70, "needs_poi":true,
      "time_hint":"~黄昏", "explicit":false }
    // 例: { "type":"pickup","intent":"接人","needs_poi":false,"at":"望京" }  // 无需检索的任务=锚点
  ],
  // 固定日程锚点：硬时间+地点(会议/约定/议程表)，整条行程围绕它排
  "fixed_events": [
    // { "title":"客户会议","place":"上海中心","start":"14:00","end":"16:00" }
  ],
  "clarification_needed": ["budget_total", "area_scope"]  // 非空则先追问
}
```

### 6.3 工具契约：LLM 可调用的函数（function calling）

后端把这几个函数暴露给 LLM，LLM 在循环中自主调用以"取真实数据"，从而杜绝编造：

```jsonc
// 1) POI 检索（反幻觉核心；地点只能从这里来）——用「搜索 POI 2.0」/v5/place/* + show_fields
search_poi(keywords, city="北京", types?, location?, radius?,
           show_fields="business,photos", sortrule?, page_size?)
  // /v5/place/text 关键字搜索 | /v5/place/around 周边搜索(返回 distance, 支持 sortrule=distance|weight)
  -> [{
       id, name, address, location:[lng,lat], type,
       distance,                          // 仅周边搜索返回
       business: {                        // 需 show_fields 含 business
         rating,                          // 评分（主要 餐饮/酒店/景点/影院 类目有值）
         cost,                            // 人均消费/价位（同上）
         opentime_today, opentime_week,   // 营业时间文本 → open_now 由我们按当前时间自算
         tel, tag
       },
       photos: [{ url, title }]           // 需 show_fields 含 photos
     }]

// 2) 路径/耗时（出行方式按距离自动选 步行/驾车/公交，支持多途经点；用于排程与画线）—— 路径规划 2.0
plan_route(origin, destination, waypoints[]?, mode="auto|walking|driving|transit", depart_time?, strategy="default")
  // depart_time: 该段实际出发时刻(供时间依赖估时；高德默认含实时路况，未来时刻预测若不支持则用时段经验值)
  -> { distance_m, duration_s, cost, polyline,
       legs:[{from, to, mode, duration_s, distance_m, cost, transfers?}] }  // transfers: 公交换乘信息

// 3) 距离矩阵（可选；多点定顺序时批量取两两耗时）
distance_matrix(origins[], destinations[]) -> durations[][]

// 4) 生成高德导航链接（交付用）；同时返回"App 深链(多途经点)"与"Web 链接(兜底,单途经点)"
build_amap_nav_uri(origin, destination, waypoints[]?, platform="android|ios|web")
  -> { app_uri, web_uri }
  // app_uri(多途经点；iOS/安卓 途经点参数完全相同：vian + vialons/vialats/vianames(| 分隔) + t=0 驾车):
  //   Android  amapuri://route/plan/?slat=&slon=&sname=&dlat=&dlon=&dname=&t=0&vian=N&vialons=a|b&vialats=a|b&vianames=A|B
  //   iOS      iosamap://path?sourceApplication=RoamMind&slat=&slon=&sname=&dlat=&dlon=&dname=&dev=0&t=0&vian=N&vialons=a|b&vialats=a|b&vianames=A|B
  //   差异仅: scheme 前缀不同 + iOS 必带 sourceApplication；其余(尤其途经点)一致 → 一个 helper 按平台拼前缀即可
  // web_uri(兜底,≤1途经点): https://uri.amap.com/navigation?from=lon,lat,名&to=lon,lat,名&via=lon,lat,名&mode=car&callnative=1
```

> **✅ POI 字段可得性（已查证）**：评分(rating)/人均(cost)/营业时间(opentime)/照片(photos) 走**搜索 POI 2.0** `/v5/place/*` 的 `show_fields=business,photos` 即可拿到；**距离(distance)** 仅**周边搜索** `/v5/place/around` 返回，并支持 `sortrule=distance|weight`。两个要注意的点：① **"当前是否营业"接口不直接给**，由 `opentime` 文本 + 当前时间自行判定；② 评分/人均主要在 **餐饮/酒店/景点/影院** 类目有值，其它类目可能为空——**设计上必须容忍字段缺失**。（老 v3 接口用 `extensions=all` 也能给评分/人均/距离，但**没有营业时间**，故选 2.0。）

> **✅ 多途经点导航 · iOS 与安卓都支持（已查证）**：安卓 `amapuri://route/plan/` 与 iOS `iosamap://path` **都支持多途经点，且途经点参数完全相同**（`vian` + `vialons`/`vialats`/`vianames` 用 `|` 分隔，`t=0` 驾车）；差异仅是 scheme 前缀、且 iOS 必带 `sourceApplication`。**Web 链接 `uri.amap.com/navigation` 仅支持 1 个途经点**（`via`），作兜底。用一个前端 helper 按平台拼前缀即可保证一致性。

> **✅ 网页 → 唤起 App（已查证可行，含坑）**：网页**可以**唤起手机上的高德 App。标准做法——用户点击"导航" → 用隐藏 `iframe`/`location` 触发 App 深链 `amapuri://...` → 设 ~2s 定时器，若页面仍可见（没跳成）→ 兜底打开 `https://uri.amap.com/navigation?...&callnative=1`（H5 地图，自身也会再尝试唤起原生）。**三类环境**：① 手机普通浏览器 = 主路径（深链多途经点）；② **微信内置浏览器会屏蔽 `amapuri://` 自定义 scheme**——检测到微信 UA 就提示"右上角 → 在浏览器打开"或直接给 Web 链接；③ 桌面端无 App = 直接给 `uri.amap.com` Web 地图链接。**唤起必须由用户点击触发，不能自动跳转。**

### 6.4 Prompt 系统设计：稳定 & 正确的意图/关键词抽取

LLM 抽取天生有**方差**（同样的话，多跑几次结果不同）和**幻觉**（编出用户没说的约束）两个风险。我们不靠"写一个好 prompt"硬扛，而是用一组工程机制把它压到"稳定 + 忠实"。

**6.4.1 一条"冷抽取 / 暖表达"分离的 Prompt 链（单一职责）**

不要用一个大 prompt 既理解又规划又讲解；拆成多个各管一件事的小 prompt，分别设温度与输出格式：

| 阶段 | 职责 | 温度 | 输出 |
|---|---|---|---|
| P1 意图抽取 | 话语(+历史) → Intent Object | 低 0–0.2 | 严格 JSON（schema 约束）|
| P2 澄清决策 | 是否追问 & 问什么 | 低 | `{ask:bool, question}` |
| P3 检索词生成 | 每个 slot → 高德查询参数 | 低 | `{keywords,type,around,radius,filters}` |
| P4 选点 & 理由 | 在**真实候选**里择优 | 低–中 | `{picks, reasons}`（只能引用传入的候选）|
| P5 行程叙述 | 共情解释"为什么这么排" | 中 0.5–0.7 | 自然语言（流式）|
| P6 重规划增量 | 反馈 → 对 Intent 的 patch | 低 | JSON diff |

> 关键：抽取/检索/选点全程"冷"（低温、结构化、可复现），只有 P5 叙述"暖"。这条分离是稳定性的根。

**6.4.2 受控词表（抗漂移 + 可映射）**
- `vibe_tags`/`avoid_tags`/`desired_categories` 从一张**固定标签表**里选（半封闭，留 `other` 逃生口）——避免"安静/宁静/清净"这种同义漂移。
- 配一张 **标签 → 高德检索关键词 / POI 类型** 映射表（如 `治愈→{公园,书店,咖啡,美术馆}`、`mood=疲惫→avoid{排队,嘈杂}`）。把"用户的感受"和"该搜什么"**解耦**：模型只负责贴标签，搜索词由映射表决定 → 跨次复现、可调而不动模型。

**6.4.3 显式 vs 推断 + 出处标记（抗幻觉）**
- 每个字段标 `explicit:true/false`（推断项再带 `confidence`）。
- 铁律：**绝不编造"显式约束"**；推断项必须能被用户看见（→ "我想你需要"卡）并在关键处确认。
- 缺关键字段或低置信 → 落入 `clarification_needed`（交给 P2）。

**6.4.4 严格 schema + 校验/自修复闭环**
- 用 JSON 输出 / function calling 强制结构；落地用 pydantic / JSON Schema 校验。
- 校验失败（格式错，或语义错如结束时间早于开始）→ 把错误回灌、让模型**修一次**（限定重试次数）。

**6.4.5 Few-shot 范例（最大杠杆）**
- 覆盖：模糊情绪 / 具体有序 / 混合 / 歧义 / 自相矛盾 / 多意图；含**负例**（别编地名、别过度追问）。
- 范例 + 系统提示固定，叠加 **DeepSeek prompt caching** → 又稳又省。

**6.4.6 多轮状态 = 增量更新**
- Intent Object 作为跨轮**持久状态**；每轮产出**对它的 patch**（尤其重规划"太远了换一个"），不从头重抽，避免丢失已有约束。

**6.4.7 安全默认值（应用但显式告知）**
- `start=当前位置`、`transport=auto`、`time=now` 等默认值直接用，但 UI 写出来"可改"，不静默替用户做主。

---

## 7. 核心模块详解

### 7.1 意图理解与澄清（FR-1/FR-2 · 惊艳主战场 + 显智能）
- LLM 把用户话语解析为 §6.2 的 Intent Object；**情绪型需求**重点填充 `implicit_preferences`。
- **起点确定（两种来源）**：默认取用户**当前位置**——① 浏览器定位获取，或 ② 从话语中提取（如"从国贸出发"）；并**稍作确认**（"默认从你当前位置出发，对吗？"），用户可随时改。
- **城市与出行方式不写死**：城市由定位/话语推断，**可泛化到北京以外**；出行方式按相邻两点距离自动选（近则步行、远则驾车/公交）。
- **任务识别 + 停留时长**：识别用户要做的"任务"（吃饭/购物/打球/接人/开会…），按**任务类型库**给默认停留时长（吃饭~1h、打球~1.5h…）与必要的提前量（赶会议/航班），用户可改。
- **固定日程锚点**：会议/约定这类"某时某地"的硬事件（含用户给的议程表）作为锚点，整条行程围绕它排；可指定日期（多日为扩展）。
- `clarification_needed` 非空且会显著影响方案时**主动追问**（合并成自然的一句，避免审讯感）。**澄清本身是核心展示点**——在复杂/模糊场景里"会问对问题"最能体现系统的理解力与通人性。
- 展示设计（**两类输入 → 两种卡片**，让"模型读懂了"可见）：
  - **模糊/情绪型** → "**我想，你需要的是…**"卡片：展示推断出的 mood + vibe 标签（想要 / 避免）；
  - **具体/清晰型** → "**我听明白了**"卡片：展示解析出的**有序行程**（1→2→3→终）+ 关键约束的确认清单（时间/餐/终点），少推断、重确认。

### 7.2 广撒网候选检索 grounding（FR-3 · 反幻觉地基 + 过度生成）
- **先广后精**：对每个 slot（见 §7.3 步骤①）用 `search_poi`（`/v5/place/*`，`show_fields=business,photos`）取**多个真实候选**（每槽 N≈5~10），而非只取一个——给后面的"组合择优"留出空间。
- **地理先验**：检索带位置约束（`around` 锚在 起↔终 的走廊 / 选定区域，配 `radius`），让不同槽的候选**能被组合成不绕路的路线**，而不是散落全城。
- **筛选/排序在拿到候选后由我们做**：`sortrule` 先按距离/综合排；评分/人均/营业时间用于过滤与展示；**"当前是否营业"由 `opentime` + 当前时间计算**。字段可能缺失（非餐饮类目常无评分/人均），逻辑需容错。
- 空结果兜底：放宽关键词或扩大半径重试；仍无则坦诚告知并给替代方向。

### 7.3 路线组合、可行性与排序（FR-4 · 先广搜 → 组合 → 择优给方案）

是的——**先广撒网搜一批地点、再组合成若干条可行路线、择优给用户**，正是这里的核心流程：

```
Intent Object
   │  ① slot 分解：把一天拆成要填的"槽"（brunch / 安静处 / 黄昏观景 …）
   ▼     含固定起终点 + 时间锚点（brunch≥11:00、日落≈19:10 …）
[② per-slot 广撒网检索]  每槽搜 N 个真实候选（§7.2，带地理先验）
   ▼
[③ 批量距离矩阵]        对候选并集一次性取两两耗时（便宜）；不给每个组合都算全程
   ▼
[④ 组合 & 剪枝]         每槽取一个组成候选路线 → 定序(尊重锚点/时间窗)
   ▼                     → 算可行性(营业/饭点/总时长/不倒车) → 砍掉不可行/太绕的
[⑤ 打分 & 选 top-K]     按 偏好契合 + 效率 + 时间舒适 + 方案间差异度 排序
   ▼                     → 1 条主推 + 1~2 条"性格不同"的备选(更轻松/更高效/有亮点)
[⑥ 渲染所选]            仅对要展示的路线算全程 plan_route(画线) + 最终时间轴
   │
   └ 用户选择/不满 → 回 ④，或对某槽重检索(② 缩半径/换词) → 重排（接 §7.5）
```

- **规模小好算**：槽 3~5、每槽候选 ~5，组合空间不大，可枚举或采样组合，**不上重型 TSP 优化器**。
- **成本控制**：③ 用一次**距离矩阵**批量估时做可行性与排序；⑥ 只对最终要展示的 1~3 条算全程路线与 polyline，省高德调用。
- **可行性优先**：④ 先用真实营业时间 + 真实耗时 + 饭点把不可行组合砍掉，绝不让 LLM 口算时间。
- **给方案的方式**：默认**主推 1 条**（最合当下需求），另给 1~2 条**特征鲜明的备选**（"更轻松"/"更高效"/"含某亮点"），一键切换——既不淹没用户、又给选择感（plan 面板可加"备选路线"切页）。
- **两类输入自适应**：**具体型**多数槽被用户钉死、候选少、几乎不排序（主要做可行性校正与卡点）；**模糊型**才充分发挥"过度生成 → 择优"。
- **任务=槽**：上面的"槽"就是"需要选地点的任务"（`needs_poi:true`）；接人/回家这类无需检索的任务直接作为锚点入排程。
- **分段多模式 + 费用 + 换乘**：每段单独选 步行/驾车/公交（可混合），各段给出耗时、费用与（公交的）换乘信息。
- **时间依赖 ETA（拥堵感知）**：每段以"上一任务结束时刻"为出发时间估时；高德默认含实时路况，靠后的段用"时段拥堵经验值"修正（若高德支持未来时刻预测则启用，当前不确定，见 §6.3 `depart_time`）。
- **可行性 & 冲突检测**：输出完整时间表，并明确判断"任务能否全部完成 / 是否有时间冲突 / 是否赶得上固定日程"，不行就提示并给取舍建议。

### 7.4 表达与可视化交付（FR-5/6/7 · 展示度主战场）
- LLM 生成有共情、解释"为什么这么安排"的话术，**流式**输出。
- 前端：高德地图画线（路线**动画绘制**入场，克制优雅）+ 标点信息窗（带照片/评分/营业时间）+ 时间轴卡片 + 方案卡片。
- "一键导航"按钮 → `build_amap_nav_uri`，按 §6.3 的"网页→唤起 App"方案跳转。

### 7.5 交互式重规划（FR-8 · 核心展示点）
- 把用户反馈解析为对 Intent Object 的**增量修改**（改约束/换某点），只重跑受影响的步骤，地图与时间轴局部更新。
- 这是最能"秀智能"的环节：现场一句"那个太远了换一个 / 换个更安静的 / 预算压低点"，系统理解并重排——复杂场景下的规划能力和通人性在这里集中体现。

### 7.6 语音实时交互（FR-9 · 炫技进阶）
- 接 Qwen-Omni-Realtime：前端经 WebSocket/WebRTC 推流式音频，模型实时返回语音；与文本链路共用同一 Intent/规划后端。
- 范围控制：先保证文本链路完整，再叠加语音；语音失败可降级回文本。

### 7.7 增值信息 / 贴心提示（FR-16 · 情绪需求层）
- 在行程之上叠一层"贴心"：**天气感知**（户外/观景看天气，雨天给替代）、**提醒**（营业截止/闭园前赶到、错峰出行）、**顺带推荐**（路过的小亮点）、**相关链接**（景点/餐厅详情）。
- 由 P5 叙述阶段顺带产出，控制信息量、不喧宾夺主。

---

## 8. 技术选型（已查证，含价格）

### 8.1 大模型（用户偏好 DeepSeek-V4，确认成立且极便宜）

| 模型 | 定位 | 价格（约 USD/百万 token，缓存未命中输入 / 输出）| 说明 |
|---|---|---|---|
| **DeepSeek-V4-Flash** | 主力 | ~$0.14 / ~$0.28 | 支持 **function calling**、JSON 输出、1M 上下文、OpenAI/Anthropic 兼容 API；Demo 成本可忽略 |
| DeepSeek-V4-Pro | 难 case 兜底 | ~$0.435 / ~$0.87（5/31 前限时折扣）| 复杂情绪推断/长链路规划时备用 |

> `deepseek-chat`/`deepseek-reasoner` 现映射到 V4-Flash 的非思考/思考模式，旧名 2026-07-24 停用——直接用新名。OpenAI 兼容 → 用 `openai` SDK 改 `base_url` 即可。

### 8.2 地图（高德，国内最佳 + 北京场景）
- **Web 服务 API**（后端）：**搜索 POI 2.0** `/v5/place/*`（`show_fields=business,photos` 取评分/人均/营业时间/照片）+ **路径规划 2.0**（驾车/步行/公交按距离选，支持多途经点/多路线/避让限行）。
- **JS API 2.0**（前端）：地图、路线动画、标点（动效保持克制；可选 Loca）。
- **URI API / App 深链**：唤起高德 App 做（多途经点）路线导航（见 §6.3 工程方案）。
- Key 申请见高德控制台 `console.amap.com`：①后端用「**Web 服务**」Key（POI/路径）；②前端用「**Web 端(JS API)**」Key（**2021-12 起须配安全密钥 jscode**，建议设域名白名单）；唤起 App 的 URI/深链**无需 Key**。坐标系为 **GCJ-02**，全链路统一。

### 8.3 语音（进阶）
- **Qwen-Omni-Realtime（阿里百炼）**：流式音频进、文本+音频出，WebSocket/WebRTC，北京地域可用；新版 Qwen3.5-Omni-Realtime 邀测**限时免费**，适合学生 Demo。注意这是与 DeepSeek 不同的供应商（混合栈，可接受）。

---

## 9. 演示脚本（端到端 · 标注炸场时刻）

### 脚本 A —— 清晰具体型
> 用户："下午先去南锣鼓巷逛逛，然后在附近找家评分高的烤鸭店吃晚饭，最后去景山公园看日落俯瞰故宫，晚上回望京的家。"
- 解析显式 POI 与顺序 → grounding 找真实烤鸭店（✨展示评分/营业时间/照片）→ 排程卡住日落时刻 → 画线+时间轴 → ✨一键唤起高德导航。

### 脚本 B —— 模糊情绪型（灵魂演示，含主动澄清）
> 用户："今天有点累，想找个安静的地方待着，顺便吃个不赶时间的 brunch，傍晚想看看城市的样子，晚上回望京的家。"
- ✨**"我想你需要"卡**：`心情=疲惫/想静 · brunch(11点后,慢节奏) · 安静=公园/书店/咖啡 · 黄昏=城市观景 · 终点=望京`
- ✨**主动澄清一句**（展示理解力与通人性）："默认从你当前位置（国贸附近）出发；想在城里转还是去近郊？人均预算大概多少？"
- grounding 出真实的 brunch 店 / 安静去处 / 观景点 → 排程（11:30 brunch→静处→黄昏观景→回望京）→ ✨地图动画画线 + 时间轴 + 共情讲解 + 一键导航。

### 脚本 C —— 现场互动（交互式重规划，核心展示）
> 在脚本 B 基础上，用户追加："那个观景点太远了，换近一点的。" → ✨系统**增量重排**、地图与时间轴实时更新，并解释改动理由。
> 可连续追加："顺便把 brunch 换成更安静的。"——展示多轮、复杂约束下的稳定规划能力。

### 脚本 D —— 出差 / 开会型（固定日程锚点 · 复杂行程卖点）
> 用户："明天上午 10 点到上海中心开会，开到 12 点；下午 2 点在浦东软件园还有个会。中间帮我安排午饭，再找个能安静待半小时的地方，两个会之间别太赶。"
- ✨**"我听明白了"卡 + 固定日程**：锚点① `10:00–12:00 上海中心 会议`、锚点② `14:00– 浦东软件园 会议`（硬约束）；中间任务：午饭 + 安静歇脚。
- 围绕两个会议**倒排**：12:00 散会 → 真实午饭店（近、出餐快）→ 安静咖啡/书店歇 30 min → 含路程与提前量，卡 14:00 前到浦东。
- ✨**可行性结论**：明确告诉用户"时间够 / 不够"；紧张就提示砍掉歇脚或换更近的午饭——展示"带硬日程的复杂一天"也能稳稳排好。

---

## 10. 质量自检（内部保障，非对照实验）

定位是**产品展示**，不做对照实验；以下是保证 Demo 不翻车的内部质量自检：
- **地点真实性**：推荐 POI 均来自高德实时检索（结构上即保证）；抽检"存在且信息完整"。
- **行程可行性**：自动校验时间线自洽（无营业时间冲突、无负时间间隔、用餐点落在合理时段）。
- **意图命中**：对预置的模糊/情绪 query，人工过一遍"氛围/品类是否抓准"。
- **体验流畅**：首 token 延迟、整轮规划耗时、空结果兜底是否生效、唤起 App 是否成功（含微信兜底）。

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| 幻觉行程（编店名/时间兜不拢）| 强制 grounding + 真实耗时校验（§7.2/7.3）——全系统的组织主线 |
| 唤起 App 导航的途经点上限 | App 深链 `amapuri://route/plan`（Android，`vian`/`vialons`/`vialats`/`vianames`）支持多途经点；`uri.amap.com` Web 链接仅 1 途经点，作桌面/无 App 兜底 |
| 微信内置浏览器屏蔽自定义 scheme | 检测微信 UA → 引导"在浏览器打开"或直接给 `uri.amap.com` Web 链接；非微信环境走深链 + 超时兜底 |
| POI 评分/人均/营业时间字段缺失 | 用搜索 POI 2.0 `show_fields` 获取；字段可能为空（非餐饮类目），逻辑容错；营业状态由 `opentime` 自算 |
| 高德 配额/Key 限制、检索为空 | 调用缓存 + 放宽重试 + 降级提示；申请足够配额 |
| 现场演示翻车（网络/接口抖动）| 内置预置脚本 + 关键结果可缓存回放；演示走"稳定脚本"，自由输入作为加演 |
| 语音范围蔓延 | 已降级为进阶项；文本链路优先且自洽，语音可缺省 |
| 成本失控 | DeepSeek-V4-Flash 极廉 + 缓存；监控调用量 |

---

## 12. 附录：API & 参考清单

- DeepSeek 价格：https://api-docs.deepseek.com/quick_start/pricing
- DeepSeek V4 发布说明：https://api-docs.deepseek.com/news/news260424
- 高德 搜索 POI 2.0（show_fields/business/photos）：https://lbs.amap.com/api/webservice/guide/api-advanced/newpoisearch
- 高德 路径规划 2.0：https://lbs.amap.com/api/webservice/guide/api/newroute
- 高德 URI API（唤起 App，Web 链接单途经点）：https://lbs.amap.com/api/uri-api/summary ｜ 路线：https://lbs.amap.com/api/uri-api/guide/travel/route
- 高德地图手机版（App 深链 `amapuri://`，多途经点）：https://lbs.amap.com/api/amap-mobile/guide/android/route
- 高德 JS API 2.0：https://lbs.amap.com/api/javascript-api-v2/summary
- Qwen-Omni-Realtime 实时语音：https://help.aliyun.com/zh/model-studio/realtime

---

### 附：队友需求书覆盖对照（任务需求.md / 需求.md）

| 队友需求 | 本 Proposal 落点 |
|---|---|
| 关键信息提取（出发/目的/途经/时间/交通/预算）| §6.2 Intent Object + §6.4 抽取机制 |
| 识别"任务" + 按类型估停留时间/提前量 | FR-11 · §7.1 · §7.3（任务类型库）|
| 信息不足主动追问 | FR-2 · §7.1 |
| 子任务拆解 + 结构化建模 | §6.2 `tasks` · §7.3 ① |
| 多候选生成 + 距离/评分/营业/顺路筛选 + 多方案组合 | §7.2 + §7.3（广搜→组合→多方案）|
| 多方案展示 + 多轮反馈 + 上下文记忆 + 重规划 | FR-8 · §7.5（会话内记忆已含；跨会话个性化 → FR-17 🅵 预留）|
| 分段路线 + 每段交通/耗时/费用 + 换乘 + 混合 | FR-13 · §7.3 · §6.3 |
| 停留时间估计 + 可改 + 完整时间表 + 冲突判断 | FR-11 / FR-15 · §7.3 |
| 时间依赖路线（按出发时刻 + 拥堵）| FR-14 · §6.3 `depart_time`（部分依赖 API）|
| 固定日程 / 议程表 + 指定日期 / 多日 | FR-12 · §6.2 `fixed_events` |
| 情绪需求：信息推送 / 其他推荐 | FR-16 · §7.7 |
| 输出：日程表 / 地图 / 时间表 / 链接 | §7.4 · FR-5 |

### 已确认（截至 v0.9）
- [x] 项目命名：**RoamMind**
- [x] 用户登录/记忆持久化：**本期不做**（无需持久化）
- [x] 演示终端：**高展示度纯网页**，简洁克制现代 AI 产品风（OpenAI/Anthropic 类），不模仿车机
- [x] 多途经点导航：**可行**——App 深链 `amapuri://route/plan/`（见 §6.3）
- [x] 网页→唤起 App：**可行**——iframe + ~2s 超时兜底 `uri.amap.com&callnative=1`；微信内需引导
- [x] POI 字段：**搜索 POI 2.0** `/v5/place` + `show_fields=business,photos` 取评分/人均/营业时间/照片/距离（§6.3/§7.2）
- [x] 去掉"vs 裸模型对照"（定位为产品展示）
- [x] FR-2 主动澄清、FR-8 交互式重规划 列为**核心展示点**
- [x] **不绑定城市 + 出行方式按距离动态选**（演示以北京为主，可泛化到其它城市）
- [x] **起点两种来源**：默认当前位置（浏览器定位 / 从话语提取）+ 稍作确认
- [x] UI 改版：左侧对话用圆角卡片包围、顶栏白底抬升区分、时间轴卡片加品类 emoji、slogan 改为"把行程安排好"
- [x] **Prompt 系统机制**（§6.4：冷抽取/暖表达分离、受控词表、显式 vs 推断、schema 自修复、few-shot+缓存、多轮增量）
- [x] **路线生成流程**（§7.3：广搜 → 组合 → 可行性剪枝 → 择优，主推 + 备选）已设计
- [x] **已融合队友需求书**：任务建模 / 固定日程+议程表 / 分段多模式+费用+换乘 / 时间依赖ETA / 可行性冲突检测 / 增值信息（见上"覆盖对照"表）
- [x] FR-14 时间依赖 ETA、FR-17 本地记忆/个性化 → **🅵 预留**（设计保留、本期不开发）
- [x] 多日 = 扩展；**"复杂行程（出差/开会）"作为第二卖点场景**（脚本 D），与休闲场景不冲突

### 待下一步细化（进入实现/打磨阶段）
- [x] iOS/安卓 scheme 已对齐：途经点参数一致（`vian`/`vialons`/`vialats`/`vianames`），仅前缀不同 + iOS 需 `sourceApplication`（见 §6.3）
- [ ] 微信内置浏览器的兜底文案/交互细节
- [ ] 高德 Key 申请、配额，及坐标系 GCJ-02 统一
- [ ] 视觉设计稿 / 设计系统选定（参考 OpenAI/Anthropic 简洁风）
